## Introduction

PostgreSQL-Vector is a PostgreSQL package built with pgvector support, suitable for vector search and AI-related workloads.

## Features

- Provide a PostgreSQL runtime with pgvector extension support.
- Support persistent data storage and configurable credentials.
- Expose a dedicated service port for external database access.
