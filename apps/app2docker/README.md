## 产品介绍

App2Docker 是一个可视化工具，用于将本地应用或运行环境快速转换为 Docker 镜像，适合需要通过界面完成构建、打包与部署的场景。

## 主要功能

- 通过 Web 界面配置镜像构建流程，减少手写 Dockerfile 的成本。
- 支持将生成的镜像直接推送到镜像仓库或在本地环境运行。
- 将构建过程中的数据和配置保存到挂载目录，便于持久化和排查问题。
- 提供对宿主机 Docker 能力的直接访问，便于执行镜像构建任务。
