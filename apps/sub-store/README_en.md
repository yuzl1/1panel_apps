## Introduction

Sub-Store is an advanced subscription management tool for subscription aggregation, protocol conversion, and rule processing workflows.

## Features

- Merge multiple subscription sources into a single entry point.
- Convert configurations across modern and legacy proxy protocols.
- Rename, filter, and sort nodes with custom scripts.
- Support frontend access through a configurable backend path.
