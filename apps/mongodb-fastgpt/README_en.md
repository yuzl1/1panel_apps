## Introduction

MongoDB-FastGPT is a MongoDB 5.0.32 local app package tailored for FastGPT scenarios, with startup logic that initializes a single-node replica set automatically.

## Features

- Provide a MongoDB document database runtime.
- Initialize a replica set automatically for applications that require replica set mode.
- Persist database data in mounted storage directories.
- Configure administrator credentials and service port during installation.
