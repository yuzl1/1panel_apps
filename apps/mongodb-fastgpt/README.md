## 产品介绍

MongoDB-FastGPT 是一个面向 FastGPT 场景的 MongoDB 5.0.32 本地应用包，包含自动初始化副本集的启动逻辑，适合需要单节点副本集能力的部署场景。

## 主要功能

- 提供 MongoDB 文档型数据库运行环境。
- 自动初始化副本集，便于兼容依赖副本集能力的应用。
- 支持持久化存储数据库数据目录。
- 支持通过安装参数设置管理员账号、密码和服务端口。
